// XIndex configuration (read/write arrays when index has X)
xindex_select_method = resolution ;
disable_xindex = read ;

// Selectively enable/disable xprop on different modules|instances|hierarchies
// When using a cfg file, default is xpropOff, so configure on 'system' sub-hier
instance (0) { TestDriver.testHarness.system } { tmerge } ;
